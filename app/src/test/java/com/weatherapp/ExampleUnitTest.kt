package com.weatherapp

import org.junit.Test
import org.junit.Assert.*

/**
 * Placeholder unit test — story-specific tests added in each subsequent story.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
